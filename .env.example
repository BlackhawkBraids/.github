# Environment variables for BlackhawkBraids checkout server
# Copy this file to .env and fill in the values — never commit .env to source control.

# Stripe secret key (from https://dashboard.stripe.com/apikeys)
# Use the test key (sk_test_...) during development.
STRIPE_SECRET_KEY=sk_test_YOUR_KEY_HERE

# Public base URL of the server (used for Stripe success/cancel redirect URLs)
BASE_URL=http://localhost:3000

# Port the Express server listens on (defaults to 3000 if not set)
PORT=3000
