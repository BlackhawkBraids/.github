# Environment variables for BlackhawkBraids checkout server
# Copy this file to .env and fill in the values — never commit .env to source control.

# ── Stripe ──────────────────────────────────────────────────────────────────

# Stripe secret key (from https://dashboard.stripe.com/apikeys)
# Use the test key (sk_test_...) during development.
STRIPE_SECRET_KEY=sk_test_YOUR_KEY_HERE

# Stripe webhook signing secret (from https://dashboard.stripe.com/webhooks)
# Generated when you register the /api/webhook endpoint in the Stripe dashboard.
# On Render: set as a secret environment variable (never store in render.yaml).
STRIPE_WEBHOOK_SECRET=whsec_YOUR_WEBHOOK_SECRET_HERE

# ── Server ───────────────────────────────────────────────────────────────────

# Public base URL of the deployed backend (used for Stripe success/cancel redirects).
# Production (Render):  https://blackhawkbraids-api.onrender.com
# Development:          http://localhost:3000
BASE_URL=http://localhost:3000

# Port the Express server listens on.
# Render sets PORT automatically — do not override it in the Render dashboard.
PORT=3000

# Node environment: "production" on Render, "development" locally.
NODE_ENV=development

# ── Frontend (Vercel) ─────────────────────────────────────────────────────────

# Full URL of the Render backend, used by vercel.json rewrites so that
# fetch("/api/...") calls from the Vercel-hosted frontend are forwarded to Render.
# Set this in the Vercel project settings → Environment Variables.
RENDER_BACKEND_URL=https://blackhawkbraids-api.onrender.com
